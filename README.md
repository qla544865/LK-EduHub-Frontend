```
## Cài đặt
npm install

## Chạy
npm run dev

## Build
npm run build
```

##### Note: branch đặt tên của ai up code lên
```
git init
git add README.md
git commit -m "commit"
git branch -M <branch>
git remote add origin https://github.com/qla544865/LK-EduHub-Frontend.git
git push -u origin <branch>
```