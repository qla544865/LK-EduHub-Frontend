// src/screens/Behavior/BehaviorScreen.js
import { View, Text, TouchableOpacity } from "react-native";

export default function BehaviorScreen({ navigation }) {
  return (
    <View style={{ flex: 1, padding: 20 }}>
        <Text>Đánh giá hạnh kiểm</Text>
    </View>
  );
}
